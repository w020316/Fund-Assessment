fastapi==0.115.12
uvicorn[standard]==0.34.2
requests==2.32.3
pandas==2.3.3
numpy==2.5.1
loguru==0.7.3
pydantic==2.13.4
python-dotenv==1.1.0
beautifulsoup4==4.13.4
html5lib==1.1
pyyaml==6.0.2
apscheduler==3.11.0
akshare==1.16.94
tushare==1.4.18
tdxpy==0.2.7
mootdx==0.9.11
litellm>=1.40.0
httpx>=0.27.0
json-repair>=0.25.0
# 借鉴 la-deps/slowapi(1.0K Star)FastAPI 限流中间件
# 保护 LLM 高成本端点,纯Python无Rust依赖
slowapi>=0.1.9
# SnowNLP 为可选依赖(借鉴 isnowany/snownlp 6.5K Star 中文情感分析)
# 纯Python但setup.py在部分环境安装慢,改为运行时可选导入
# 代码已实现降级:未安装时自动退化为纯关键词法(news_aggregator.py)
# 本地开发: pip install snownlp 即可启用语义分析增强
# Render部署: 不安装,使用关键词法(稳定优先)
